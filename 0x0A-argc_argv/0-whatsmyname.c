#include <stdio.h>
/**
 * main - Entry point.
 * @argc: int.
 * @argv: list.
 * @n: Maximum number of bytes from src.
 *
 * Return: Always 0 (Success).
 */
int main(int argc, char const *argv[])
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}
